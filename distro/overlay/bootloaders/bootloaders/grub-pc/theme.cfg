# GRUB's theme.cfg color variables accept named terminal colors, not the
# six-digit hex values used by theme.txt. Keep the Shinobi palette in the
# graphical theme while using portable GRUB menu colors here.
set color_normal=light-gray/black
set color_highlight=white/blue

set theme=/boot/grub/theme/theme.txt
